/* import { createSelector } from "reselect";
import { TAppState } from "..";

type TGlobalState = {
  feedsGlobalStore: {
    feedsList: Array<any>;
    feedsCount: number;
    loading: boolean;
    error: string;
  };
};

export const getFeedsList = createSelector(
  (state: TGlobalState): Array<any> => state.feedsGlobalStore.feedsList,
  (feedList: Array<any>): Array<any> => feedList
);

export const getFeedsCount = createSelector(
  (state: TGlobalState): number => state.feedsGlobalStore.feedsCount,
  (feedsCount: number): number => feedsCount
);

export const getFeedsLoading = createSelector(
  (state: TGlobalState): boolean => state.feedsGlobalStore.loading,
  (loading: boolean): boolean => loading
);
export const getFeedsError = createSelector(
  (state: TGlobalState): string => state.feedsGlobalStore.error,
  (error: string): string => error
); */

/* export const getFeedsList = (state: TAppState) => {
  (state: TAppState) => ,
  (feedsCount: number): number => feedsCount
}; */

/* 
export const getFeedsLoading = createSelector(
  (state: TAppState): boolean => state.feedsGlobalStore.loading,
  (loading: boolean): boolean => loading
);
export const getFeedsError = createSelector(
  (state: TAppState): string => state.feedsGlobalStore.error,
  (error: string): string => error
);
 */
